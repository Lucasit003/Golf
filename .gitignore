node_modules
dist
.DS_Store

# Screenshot tool output
/shots

# MediaPipe wasm — copied from the npm package at build time (see tools/copy-wasm.mjs).
# The pose *model* under public/models IS committed; the wasm is reproducible.
/public/mediapipe
*.local
.env
.env.*

# Test footage — large, and it's Lucas's video
/footage
*.mov
*.mp4

# Pose fixtures ARE committed — they're the test suite.
!src/**/fixtures/*.json

# TypeScript incremental build info
*.tsbuildinfo

# Generated self-contained artifact build
contour-artifact.html
